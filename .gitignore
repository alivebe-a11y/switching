__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
.cache/
data/cache/
.env
.env.*
*.key
*.pem
# Runtime state — SQLite store (+ WAL/SHM sidecars) and legacy JSON backups
*.sqlite
*.db
*.db-wal
*.db-shm
switching.db
# Exception: the storage-compat fixture is a committed golden snapshot
# (regenerated only via scripts/build_test_fixture.py on deliberate
# schema changes). It is the ONLY .db file we want in the repo.
!tests/fixtures/*.db
paper_portfolio.json
uk_portfolio.json
t212_portfolio.json
alpaca_state.json
exit_tracker.json
skipped_signals.json
trade_memory.json
# Backup artifacts produced by scripts/backup.ps1
backups/
*.tar.gz
# Bench output produced by scripts/bench_ticker_resolver.py
# (CSV + summary per run; stays local, may contain headline text not for committing)
bench/
